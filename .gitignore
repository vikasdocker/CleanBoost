bin/
obj/
*.user
*.suo
.vs/
TestResults/
dist/*.zip
dist/*.msix
dist/*.msix.part*
dist/*.msi
dist/*.wixpdb
*AppPackages*

# Signing keys must never land in this (public) repo.
*.pfx
*.p12
*.snk

# Build outputs — regenerated by scripts/build-release.ps1.
# packaging/msix/layout keeps only AppxManifest.xml + Assets tracked.
publish/
packaging/msix/layout/*
!packaging/msix/layout/AppxManifest.xml
!packaging/msix/layout/Assets/
